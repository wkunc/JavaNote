1.@Required
    这个注解应用于bean属性的setter方法,它表明受影响的bean属性在配置时必须放在XML文件中
    否则容器会抛出一个BeanInitializationException异常
    beans.xml中


    <context:annotation-config/>

2.@Autowired(byType)
    这个注释对在哪里和如何完成自动连接提供了更多的细微控制
    setter上的@Autowired
        @Autowired注释可以在setter方法中用于自动连接bean(使用自动装配byType方式)
    属性上的@Autowired
        注释在属性上时可以免去该属性的setter方法
    构造函数上的@Autowired
        @Autowired 说明当创建 bean 时，即使在 XML 文件中没有使用 元素配置bean构造函数也会被自动连接
    @Autowired 注释意味着依赖是必须的，它类似于 @Required 注释，然而，你可以使用 @Autowired 的(required=false)选项关闭默认行为。

3.@Primary


4.@Qualifier
    //限定符用来在多个可选的bean中筛选


4.JSR-250 Annotations(@Resource，@PostConstruct 和 @PreDestroy)
    @PostConstruct 注释的方法对应init-method属性
    @PreDestroy 注释的方法对应destory-method属性

二.
    @Configuration(注释在类上)相当与给xml中编写了标签<bean></bean>
        带有 @Configuration 的注解类表示这个类可以使用 Spring IoC 容器作为 bean 定义的来源。
    @Bean