org.springframework.beans.factory.FactoryBean
这个接口是spring 对外提供对接的接口
当spring 调用getBean 时 spring 会使用 getObject() 对象

意味着每当 spring 通过getBean("FactoryBean<T>的实现类") 都会返还 T 对象

    public interface FactoryBean<T> {
        T getObject() throws Exception;
        Class<?> getObjectType();
        boolean isSingleton();
    }

典型实现 mybatis-spring 整合中的
org.mybatis.spring.SqlSessionFactoryBean 这个类

public class SqlSessionFactoryBean implements FactoryBean<SqlSessionFactory>
所以每次都会返回一个 SqlSessionFactory 对象
SqlSessionFactoryBean 中有一个 SqlSessionFactoryBuilder
SqlSessionFactory 还是由 SqlSessionFactoryBuilder.builc(Configuration );生成的